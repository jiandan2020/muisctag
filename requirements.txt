# MusicTag 依赖清单
# 安装:  python -m pip install -r requirements.txt

# ---- 核心 ----
mutagen>=1.47          # 音频元数据（MP3/FLAC/OGG/M4A/APE/WV/MPC/WMA/DSF 等）
PySide6>=6.6           # GUI 框架
requests>=2.31         # 网络请求（刮削/翻译）
Pillow>=10.0           # 封面图片处理（缩放/格式转换）

# ---- 可选 ----
# opencc-python-reimplemented>=1.1   # 繁简转换（标签清理）
# aiohttp>=3.9                       # 异步批量请求（未来扩展）
